<?php
class PzkTextLabel extends PzkObject {
	public function html() {
		return $this->value;
	}
}

$sets = [[15, a], [27, b], [30, c], [16, d]];

find($sets, 30) = O(n);

hashStore([30, c])
	$i = hash(30);
	$cell = $sets[$i];
	if($cell && $cell[0] !== 30) {
		$i++
	}
	$sets[hash(30)] = [30, c];

hashFind($sets, 30) = O(1);

	hash(30) -> 2 O(1);
	return $sets[2] O(1)
	
	30 % 7 = 2
	16 % 7 = 2 

